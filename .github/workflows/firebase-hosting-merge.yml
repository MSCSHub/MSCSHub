# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy_mscs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: npm install -g @angular/cli@latest
      - run: ng build --configuration=mscs
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_UTMCS_1BCF2 }}
          channelId: live
          projectId: utmcs-1bcf2
          target: utmscs
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
  build_and_deploy_msds:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: npm install -g @angular/cli@latest
      - run: ng build --configuration=msds --output-path dist/msds
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_UTMCS_1BCF2 }}
          channelId: live
          projectId: utmcs-1bcf2
          target: msds
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
  build_and_deploy_msai:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: npm install -g @angular/cli@latest
      - run: ng build --configuration=msai --output-path dist/msai
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_UTMCS_1BCF2 }}
          channelId: live
          projectId: utmcs-1bcf2
          target: msai
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
